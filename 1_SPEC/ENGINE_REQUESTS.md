# ENGINE REQUESTS — generated, do not hand-edit

Generated by `_tools/engine_request.py list`. Each request is its own file in
`engine_requests/` so 10 parallel sessions cannot clobber one another.

Needs the engine lock: `engine/app.js` · `engine/style.css` · the FLN monolith · the six
version stamps. Parallel-safe (no lock): `build_skill_<CODE>.py`, that game's card/art/VO/
dist/zip/receipt. Reading the engine is always fine; builds are never blocked.

## Open — 8 request(s) in 8 distinct fix(es)

- [ ] **BLOCKING** · Scaffold ladder needs a 3rd attempt: 1st wrong = hint VO only with NO hand, 2nd wrong = hint VO + hand nudge on the CORRECT option (child still answers), 3rd-attempt correct = confetti but SILENT. Today max_attempts is 2 and the 2nd wrong triggers terminalHold, which reveals the answer and dims the rest
      games: HI02H11_L02_S01   (1 report(s))
      evidence [HI02H11_L02_S01]: SME specifies this identical ladder on all 9 test screens of the HI02H11_L02_S01 redesign. Not a card fix: max_attempts is read from scaffold_rules but the rung BEHAVIOUR is hardcoded in wrongClip()/midHint()/terminalHold(), and celebrateThenAdvance(slide, revealed) always plays correct-or-reveal so a silent visual-only success is not expressible. Proposed additive keys (defaults preserve today's behaviour, so no shipped game changes): hand_on_attempt:2, reveal_on_attempt:null, silent_on_late_correct:true. Fleet-wide value: the current ladder hands the answer over one rung early.
      id: 20260916-224551-hi02h11-l02-s01-2  session: sme-lxd-matra-train
- [ ] **BLOCKING** · MATRA_BUILD module: staged transformation teach — base word, highlight the consonant, the matra travels in, consonant+matra=syllable with its own sound, remaining letters join, matra highlighted inside the finished word
      games: HI02H11_L02_S01   (1 report(s))
      evidence [HI02H11_L02_S01]: SME's core teaching idea for HI02H11_L02_S01, on 3 screens (जल→ज+ा=जा→जाल, बल→बि→बिल, कल→की→कील); mockups _SME_MOCKUPS/slide05,07,09. Not a card fix: MEET_LETTER shows one static word and DECODE_TAP (08.14a) taps pre-made aksharas — neither animates a word changing, and nothing sequences per-stage audio against a per-stage visual. Reusable by every matra lesson in the fleet. Gate each step on the previous CLIP ENDING, not a timer — [32b nocut] records fixed 1400ms advances truncating 27-35% of every reveal line. WARNING: the per-stage `onset` clip (जा / बि / की) may not synthesise — bare aksharas hard-refuse (HTTP 400 on क/न/द/त) and carriers are unreliable per letter; budget human recording.
      id: 20260916-224551-hi02h11-l02-s01-3  session: sme-lxd-matra-train
- [ ] **BLOCKING** · MATRA_FILL module: render a word with a BLANK where its matra belongs and let the child drag a matra card into it
      games: HI02H11_L02_S01   (1 report(s))
      evidence [HI02H11_L02_S01]: SME asked for this in round 1 and again in round 2 for HI02H11_L02_S01 (ज_ल→ा, पर_→ी, ह_रण→ि); mockup _SME_MOCKUPS/slide16. I substituted a wrong-matra pick and told the SME it was a substitution. Not a card fix: I checked all 59 modules in 2026.08.04b and every module in the reference engines — nothing renders a word with a slot. CRITICAL: the blank index must be into the CONSONANT SEQUENCE, not a character offset — ि is a reordering matra (stored after its consonant, drawn before it), so in हिरण a naive character-offset insert puts the matra in the wrong visual position. Render the blank in the DRAWN position.
      id: 20260916-224551-hi02h11-l02-s01-4  session: sme-lxd-matra-train
- [ ] **BLOCKING** · SORT_GENDER needs a data.hide_labels flag so tiles render the picture only, with the word still SPOKEN on tap
      games: HI02H11_L02_S01   (1 report(s))
      evidence [HI02H11_L02_S01]: HI02H11_L02_S01 redesign screen 14 (mockup _SME_MOCKUPS/slide17): SME is explicit that the word must never be displayed during the question but must be heard, which is what makes it a listening task rather than picture-matching. Not a card fix: SORT_GENDER unconditionally renders <span class="lbl">${it.word_hi}</span>. ~3 lines, additive, default false so no shipped game changes. Also note .sort-tray is flex-wrap:nowrap and overflows past ~6 tiles at 104px — that screen has exactly 6.
      id: 20260916-224551-hi02h11-l02-s01-5  session: sme-lxd-matra-train
- [ ] **BLOCKING** · TRAIN_CHROME: no shared train shell — a locomotive + N coaches that act as answer options, sort bins and word frames, with right-to-left entry, and per-coach idle/hover/correct/wrong/locked/nudge/complete states
      games: HI02H11_L02_S01   (1 report(s))
      evidence [HI02H11_L02_S01]: SME redesign of HI02H11_L02_S01 into «मात्राओं की रेल» (recommendations verbatim in the bundle's _SME_RECOMMENDATIONS.md, mockups in _SME_MOCKUPS/). Not a card fix: 6 of the 17 screens mount inside this shell and nothing in the engine draws it. Checked all 59 modules in 2026.08.04b — no themed container exists; SORT_GENDER's bins and mountTapOptions' opt-grid are the only containers and neither is skinnable from a card. NOTE for whoever builds it: capture_pages.py injects *{animation:none !important}, so any coach state that lives only in @keyframes is invisible in review decks — put the settled state on the base rule (this already cost 4 attempts on the landing letter glow).
      id: 20260916-224551-hi02h11-l02-s01  session: sme-lxd-matra-train
- [ ] MEET_LETTER needs data.examples[] so one slide can teach TWO example words one at a time, each with its own picture and matra highlight
      games: HI02H11_L02_S01   (1 report(s))
      evidence [HI02H11_L02_S01]: HI02H11_L02_S01 redesign screens 3, 5 and 7 (नाक+मटका, दिन+गति, तीर+लड़की). SME: "Keep one example visible at a time... Keep animation one by one, not all together." Not a card fix: MEET_LETTER renders slide.data.letter once and runs one autonomous demo; there is no loop. Additive — keep the single-word path byte-identical so the 11 shipped MEET_LETTER games are untouched. NOT blocking: the screens degrade to one example each.
      id: 20260916-224551-hi02h11-l02-s01-6  session: sme-lxd-matra-train
- [ ] POEM_SEARCH module: multi-line poem card with word-level hit testing, a draggable magnifying glass that magnifies the word under it, 3 sequential target rounds over the same poem, and an idle/hint guide actor
      games: HI02H11_L02_S01   (1 report(s))
      evidence [HI02H11_L02_S01]: HI02H11_L02_S01 redesign screen 15 «मात्रा खोजो» (mockup _SME_MOCKUPS/slide18). Not a card fix: SENTENCE_FIND tokenises a SINGLE sentence into chips with no lens, no round state and no per-word geometry that survives reflow. Also needs a second nudge actor alongside the hand — extend the existing nudge system with a pluggable actor ('hand'|'ghost'|'mascot') rather than a parallel timer, so the one-nudge-at-a-time and state.hintActive guards keep working. NOT blocking and DO NOT SCHEDULE YET: the SME's target word list is incomplete (4 correct answers are missing — a child tapping चमकी for ी would be marked wrong), and the target list is this module's entire data model. See §4 of the bundle's DEV_HANDOFF.
      id: 20260916-224551-hi02h11-l02-s01-7  session: sme-lxd-matra-train
- [ ] Matra highlighting inside a word is pixel-column based and therefore CANNOT work for reordering matras — needs replacing with glyph-cluster-aware highlighting
      games: HI02H11_L02_S01   (1 report(s))
      evidence [HI02H11_L02_S01]: HIGHEST-LEVERAGE ASK IN THIS BATCH — fleet-wide, every matra lesson. _matraClipCols rasterises the word and colours ink columns after the consonant, so it works for right-spacing matras (ा, ी — both verified correct on नाक and तीर) and CANNOT work for ि, which is typed after its consonant but drawn before it. Measured, not assumed: with ि added to RIGHT_SPACING_MATRAS, दिन mis-clips and the red lands on part of the द — the child is shown the wrong glyph as 'the matra'. Tried, rendered, reverted; see the bundle's engine_local/CHANGES.md. Proper fix: shape the word into per-cluster spans and recolour the matra's cluster, instead of clipping pixel columns. That also unlocks े ै ो ौ ु ू, which all currently fall back to the ◌<matra> callout.
      id: 20260916-224551-hi02h11-l02-s01-8  session: sme-lxd-matra-train
